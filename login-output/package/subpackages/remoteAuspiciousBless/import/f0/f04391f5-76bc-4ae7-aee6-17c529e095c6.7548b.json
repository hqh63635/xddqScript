[1,0,0,[["cc.TextAsset",["_name","text"],1]],[[0,0,1,3]],[[0,"SEAL_MONSTER_MAP_TODO","# 轩辕镇魔地图 — 待对接清单（人工填空）\n\n以下为策划/UI/协议确认项，代码侧已用占位或注释标出。\n\n## 资源路径\n\n- **地形远程图**：`RemoteSealMonsterPathUtil.getMapTerrainImageUrl` 默认 `remoteSealMonster/images/{imgNameMap}`，若实际为图集-only 或目录名不同，请改该函数一处。\n- **FGUI**：地块组件 `tileItem` / `tileDecorateItem` 是否暴露 `loaderIcon` 或子节点 `icon`；若命名不同，请改 `SealMonsterGroundItem` / `SealMonsterTileItem` 的 `resolveLoader`。\n\n## 数据语义\n\n- **`baseItem` 第三段 `type`** 与 **`tileEventItem.c1`**（Binder 注释 18～31）的映射表：请在业务配置或 `RemoteSealMonsterConfig` 中补枚举映射后再驱动表现。\n- **多格占地**：当前按 `baseItem` 第 8、9 段为 **spanCols×spanRows**（矩形）；`tiledToObj` 已废弃。\n\n## 交互与协议\n\n- **已处理事件**：`SealMonsterMapModel.duelEventTileList` / `markEventHandled` 使用 **tile id**（`baseItem` 首段）。`SealMonsterEnterGameResp.hasExploreGridIdList` 为协议 **gridId**，接入时须映射到 tile id 再调 `syncDuelEventTileListFromServer`（见 `RemoteSealMonsterMgr.resolveExploreTileIdsFromProtocolGridIds`）。\n\n---\n\n## 【待修】怪物事件完成后地图怪物 Spine 仍显示（2026-05）\n\n### 现象\n\n- 236608 战斗领奖 / 236604 处理事件 **成功后**，玩家 **可以走到** 原怪物占格（`eventOccupyMap` 已释放）。\n- 地图上 **`tileEventItem` 怪物 Spine 仍可见**（与角色重叠），未正确 recycle / 隐藏。\n\n### 已排除 / 已做改动（仍未彻底解决，需 Debug 续查）\n\n| 方向 | 说明 | 相关文件 |\n|------|------|----------|\n| 非「回包无列表」 | 能走上格说明 Model 占格已更新；与「未带 `hasExploreGridIdList`」不是同一问题 | `SealMonsterMapModel.rebuildEventOccupyMap` |\n| 单写单落地 | 探索状态仅信回包 `hasExploreGridIdList`；落地 `syncExploredGridIdsFromServer` → `applyExploredStateToScene` → `reconcileEventTileItemsWithModel` | `RemoteSealMonsterMgr`、`SealMonsterGameMgr` |\n| gridId→tileId | `resolveExploreTileIdsFromProtocolGridIds`（mapConfig.gridId + location 锚点） | `RemoteSealMonsterMgr.ts` |\n| 全屏战斗 hide | 打开 `RemoteSealMonsterBattleView`（`ViewType.Screen`）会 `UIMgr.closeStrackView` → `GameView.hide()`；**勿在 `onHide` 清空 `gameSceneView`**（改在 `onDispose`） | `RemoteSealMonsterGameView.ts` |\n| recycle 未摘层 | `MapScrollBase.recycleCom` 顶层事件格仅 `visible=false` 入池；已 override `SealMonsterSceneScroll.recycleCom` 从 `SealMonsterEvent` `removeChild` | `SealMonsterSceneScroll.ts` |\n| 表现兜底 | `SealMonsterTileItem.renderCom` 已处理则 hide；Spine 异步 `monsterSpineLoadGen` | `SealMonsterTileItem.ts` |\n| 回探索补刷 | `onShow` / `EVENT_ATTACK_REWARD` → `refreshExploredMapFromModel` | `RemoteSealMonsterGameView.ts` |\n\n### 建议 Debug 步骤（下次接手优先）\n\n1. **236608 回包当场打断点**（`RemoteSealMonsterMgr.applyAttackRewardResp`）  \n   - 打印 `msg.hasExploreGridIdList` 原始值。  \n   - 打印 `resolveExploreTileIdsFromProtocolGridIds` 输出 vs 点击格 `info.index` / `tileData.id`。  \n   - 若映射后列表 **不包含** 当前怪物 tile id → 修映射或服务端 id 语义（占格与 UI 会分叉）。\n\n2. **`applyExploredStateToScene` 是否执行**  \n   - `gameSceneView?.isViewValid`、`bindScene` 在领奖回调时是否为真。  \n   - `tileItems.length` reconcile 前后；对应项 `info.index` 与 `isEventTileHandled` 是否为 true。\n\n3. **回收后场景树**（战斗结束回到地图后）  \n   - 检查 `SealMonsterEvent` 下是否仍有 `tileEventItem` 子节点 / `spineNode` 下是否仍有 Skeleton。  \n   - 若 `tileItems` 已空但节点仍在 → **池化未 removeChild 或异步 Spine 回调重新挂回**（查 `SealMonsterTileItem.loadMonsterSpine`）。  \n   - 若 `tileItems` 仍有该项 → reconcile 未命中（id 不一致）。\n\n4. **Screen 叠层时序**  \n   - 断点 `UIBase.hide` / `GameView.onHide` / `SealMonsterGameScene.hide` / 战斗 `close` 后 `showStrackView` / `GameView.onShow`。  \n   - 确认 236608 回调与 `onShow` 谁先谁后；是否需在 **战斗 `close` 完成回调** 再强制 `refreshExploredMapFromModel()` 一次。\n\n5. **可选：临时日志**（修完删除）  \n   - `reconcileEventTileItemsWithModel`：移除的 tileId 列表。  \n   - `renderCom`：handled 仍 render 的 tileId（不应出现）。\n\n### 后续代码方向（待确认 Debug 结果后择一）\n\n- A. 战斗关窗链路末尾（`RemoteSealMonsterBattleView` / 结算 `closeCallback`）**显式** `SealMonsterGameMgr.inst.refreshExploredMapFromModel()`（不造数据，只补 UI 落地时机）。  \n- B. `reconcile` 后扫描 `SealMonsterEvent` 子节点，移除「已无对应 `tileItems`」的 `tileEventItem`（清历史残留）。  \n- C. 协议层与策划确认：`hasExploreGridIdList` 与 `baseItem` 首段 / `mapConfig.gridId` / `location` 锚点 **唯一约定**，避免占格用一套 id、UI 用另一套。\n\n### 约束（延续）\n\n- **禁止**用 `req.gridId` 在回包缺字段时本地 `markEventHandled` 兜底。  \n- UI 必须与回包写入的 Model 一致；仅允许「补 reconcile 时机 / 修 recycle / 修 id 映射」。\n\n---\n- **`RemoteSealMonsterGameView.onClickMap`**：现为日志；请按格子类型接弹窗、战斗、协议（须走 `RemoteSealMonsterMgr`，勿在 View 直发包）。\n- **初始镜头**：默认 boss `50000` 或第一块事件格；玩家当前位置应以服务端/协议字段替换 `SealMonsterGameMgr.resolveInitialTileId`。\n- **`btnPvg` / `btnInto` / `btnGame`**：当前均进入地图；若按钮语义不同，请拆分 handler。\n\n## 性能\n\n- 地表块数量较多（`basemapVec` 展开）；已有视口裁剪（`TiledItemBase`）。若仍吃力，可改为大块背景或降低加载粒度。\n"]],0,0,[],[],[]]